# Local-only example file for cx-plus fork (no DB secrets required here)
# Copy to .env and edit only if you have cli-specific envs; avoid placing service credentials here.

# Example: enable verbose logs for Agent Bus scripts
# AGENT_BUS_LOG=1

